<?php
/**
 * ============================================================
 * helpers.php — 辅助函数入口
 * ============================================================
 * 说明：按域拆分为 helpers.d/*.php，统一从此加载。
 * 所有函数均为全局函数，拆分后引用方式不变。
 * ============================================================ */
$__helpersDir = __DIR__ . '/helpers.d';
foreach (glob($__helpersDir . '/*.php') ?: array() as $__hf) {
    require_once $__hf;
}